<div id="filter">
    <form id="searchForm">
        <input id="searchImage" name="search" placeholder="поиск">
        <div id="searchButton"><img src="/frontend/css/images/search.png" alt="search"></div>
    </form>
    Поиск по тегам:
    <div id="tags">
        {% for tag in filter %}
            <div class="searchTags">{{ tag.name }}</div>
        {% endfor %}
    </div>
</div>